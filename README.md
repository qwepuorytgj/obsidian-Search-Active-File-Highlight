In folder view: When I click on a file, it opens in a tab, and a shadow is added to the folder for the file I'm currently viewing, making it easy to locate.

However, in search mode, after searching for keywords, several related files appear. When I click on a file, it opens in a tab, but the shadow isn't added to the search results. This means that if I want to find the file I'm currently viewing in the search results, I can only do so by filename, and I can't immediately see which file I've opened.

I would like the search mode to also display a shadow for the file I'm currently viewing.

